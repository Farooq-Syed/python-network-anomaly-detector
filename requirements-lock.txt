# Locked runtime environment (verified 2026-08-23 for reproduction).
# Core detector requirements:
numpy==2.2.6
pandas==3.0.3
scikit-learn==1.8.0
scipy==1.15.3
matplotlib==3.10.9

# Optional: only needed to (re)build the metadata subsets / reproduce the
# strict-split and cross-dataset scripts from the raw Hugging Face sources:
datasets==4.8.5
pyarrow==24.0.0
huggingface_hub==1.4.1
